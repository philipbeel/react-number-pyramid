// import React, { useEffect } from 'react';
// import { render } from '@testing-library/react';
// import { useQuery } from './useQuery';

describe('useQuery', () => {
  //   function TestComponent({ onEffect }) {
  //     const hookProps = useQuery();
  //     useEffect(() => {
  //       onEffect(hookProps);
  //     }, [onEffect, hookProps]);
  //     return <></>;
  //   }

  xit('returns the URL query object ', () => {
    // const onEffect = () => {};
    // render(<TestComponent {...onEffect} />);
  });
});
